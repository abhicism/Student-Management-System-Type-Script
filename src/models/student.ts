// Define the structure of a Student.
// This interface tells TypeScript what properties
// every Student object must have.
export interface Student {

    // Every student must have a numeric ID.
    id: number;

    // Every student must have a name.
    name: string;

    // Every student must have an age.
    age: number;

}